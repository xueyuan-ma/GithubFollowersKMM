pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "GithubFollowersKMM"
include(":GithubFollowersAndroid")
include(":shared")